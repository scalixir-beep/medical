{
  "name": "eps2-dossier-patient",
  "version": "1.0.0",
  "description": "Plateforme de gestion du dossier patient (EPS2). React + Laravel + SQLite.",
  "private": true,
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm run api\" \"npm run client\"",
    "api": "cd backend && php artisan serve --host=127.0.0.1 --port=8000",
    "client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
